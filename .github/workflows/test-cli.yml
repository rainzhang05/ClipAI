name: Test CLI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-cli:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Run CLIParserTests
        run: ./Scripts/test.sh CLIParserTests
