name: Test Queue

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-queue:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Run RequestQueueTests
        run: ./Scripts/test.sh RequestQueueTests
