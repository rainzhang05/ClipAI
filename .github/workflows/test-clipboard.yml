name: Test Clipboard

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-clipboard:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: Run ClipboardContentTests
        run: ./Scripts/test.sh ClipboardContentTests
